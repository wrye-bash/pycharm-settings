* text=auto

*.xml text

# git files - have them with LF, as I edit them via the shell (echo etc)
*.gitattributes text eol=lf
*.gitignore text eol=lf

# dict.dic apparently needs to be LF
dict.dic text eol=lf
